{
  "name": "shadow64-phase0-r11",
  "version": "0.1.0",
  "private": true,
  "description": "Shadow64 Phase 0 R11 libdragon N64 Expansion Pak map viewer skeleton",
  "scripts": {
    "prepare-libdragon": "libdragon init --branch trunk",
    "build": "libdragon make",
    "clean": "libdragon make clean",
    "validate": "bash scripts/validate_shadow64_package.sh"
  },
  "devDependencies": {
    "libdragon": "latest"
  }
}
